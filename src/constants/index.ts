import 'dotenv/config';

export const { DB_NAME } = process.env;
export const { DB_USERNAME } = process.env;
export const { COOKIE_SECRET } = process.env;
export const { CLIENT_DOMAIN } = process.env;
